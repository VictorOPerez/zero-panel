import { MagicRedeemClient } from "@/components/auth/magic-redeem-client";

export const metadata = { title: "Entrando… — Zero" };

export default function MagicPage() {
  return <MagicRedeemClient />;
}

import { PlatformView } from "@/components/platform/platform-view";

export const metadata = { title: "Centro de Control — Zero" };

export default function PlatformPage() {
  return <PlatformView />;
}

import { FollowupsView } from "@/components/followups/followups-view";

export const metadata = { title: "Followups — Zero" };

export default function FollowupsPage() {
  return <FollowupsView />;
}

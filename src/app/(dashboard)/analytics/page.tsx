import { AnalyticsView } from "@/components/analytics/analytics-view";

export const metadata = { title: "Analytics — Zero" };

export default function AnalyticsPage() {
  return <AnalyticsView />;
}
